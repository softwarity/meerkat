# Build artifacts
/bin/
/dist/
*.test
coverage.out

# Local environment
.env
*.local

# Editors / OS
.DS_Store
.idea/
.vscode/

# air dev loop
/tmp/

# embedded console build, staged by `make ui` (the .gitkeep keeps go:embed
# compiling when no console has been built)
/internal/admin/ui/dist/*
!/internal/admin/ui/dist/.gitkeep

# local db folder
data
