{
  "name": "meerkat-e2e",
  "private": true,
  "description": "Integration tests: seeded profiles exercise the access matrix in scenarios.json against a real gateway + console.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
